# Your song (optional)

Save your song here as `song.mp3`. It starts when she opens the photos and plays through the letter
and the finale. Without it, the page plays its own music-box melody, so nothing breaks either way.

The file is ignored by git on purpose: a commercial recording must not be published in a public
repository. Add it only where you host the page for her (see the main README).
